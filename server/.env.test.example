NODE_ENV=test
PORT=3002

# Tests default to `mini_notion_test` when DATABASE_URL is omitted (see src/config/env.ts)
DATABASE_URL=postgresql://mini_notion:mini_notion@localhost:5432/mini_notion_test?schema=public

JWT_SECRET=test-jwt-secret
JWT_EXPIRES_IN=7d
